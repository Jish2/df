# Linux-wide .zshrc.local config
#
# Keep Linux-only overrides here. Host-specific overrides can live in:
#   .zshrc.local##o.Linux,h.<hostname>

# Alt+Backspace: delete previous word
bindkey '\e^?' backward-kill-word
bindkey '\e[3;3~' backward-kill-word
